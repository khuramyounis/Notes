package com.khuram.notes.framework.datasource.preferences


class PreferenceKeys {

    companion object{

        // Shared Preference Files:
        const val NOTE_PREFERENCES: String = "com.khuram.notes"

        // Shared Preference Keys
        const val NOTE_FILTER: String = "$NOTE_PREFERENCES.NOTE_FILTER"
        const val NOTE_ORDER: String = "$NOTE_PREFERENCES.NOTE_ORDER"
    }
}